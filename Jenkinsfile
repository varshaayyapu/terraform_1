pipeline{

  agent{
    label 'slave'
    
      }
      
        stages{
          stage("checking terraform installation")}
            steps{
            
              sh "terraform -version"
              }
             }
           }
        }
